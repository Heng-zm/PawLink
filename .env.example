# API server
PORT=4174
SESSION_DAYS=30
DATA_FILE=./server/data.json
MAX_ANALYTICS_EVENTS=50000

# Optional frontend public origin. Set this before `npm run build` when the
# published profile host is different from the current browser origin.
# VITE_PUBLIC_BASE_URL=https://your-domain.example
